<% provide(:title, I18n.t("brand.company") + "\s| #{@blog.subject.titleize}") %>

<%= div_for @blog do %>
  <%= @blog.subject.titleize %>
  <br>
  <%= @blog.body %>
<% end %>
<%= link_to "Edit", edit_blog_path(@blog) if user_signed_in? %>
