<h2>Mmm...</h2>
<% if @failed == 'no_account' && @provider %>
  <p>
    <%= "You don't have an account connected to #{Authentication.provider_name(@provider)}." %>
  </p>
<% else %>
  <p>
    Something went wrong
    <% if @provider %>
      <%= "with #{Authentication.provider_name(@provider)}" %>
    <% end %>
  </p>
  <p>
    Please try again.
  </p>
<% end %>
