<!DOCTYPE html>
<html class="no-js" lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="content-type" content="text/html; charset=UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="user-scalable=no, width=device-width, initial-scale=1, maximum-scale=1, minimum-scale=1">
    <meta name="keywords" content="<%= brand_name %>">
    <meta name="author" content="<%= brand_name %>">
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : I18n.t('brand.name') %>">
    <meta property="og:type" content="website">
    <!-- <meta property="og:image" content=""> -->
    <meta property="og:site_name" content="<%= brand_name %>">
    <link rel="image_src" href="<%= image_url 'images/verde-circle-logo-name-centered.png' %>">
    <meta name="robots" content="index, follow">
    <title><%= content_for?(:title) ? yield(:title) : view_bag_title %></title>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
    <!--[if lt IE 9]
      <%= javascript_include_tag 'ie', 'data-turbolinks-track' => true %>
    -->
    <%= csrf_meta_tags %>
    <%= javascript_include_tag 'head', 'data-turbolinks-track' => true %>
    <%= headjs_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= yield :head %>
    <%= render 'layouts/airbrake' %>
  </head>
  <body class="controller__<%= controller_name %> action__<%= action_name %>" data-user="{<%= %Q("cu":{"i":"#{current_user.id if user_signed_in?}","un":"#{current_user.username if user_signed_in?}"}) %>}">
    <%= render 'layouts/navbars/communitycenter' %>
    <div class="container-fluid">
      <div class="errors__container">
        <%= render 'layouts/messages' %>
      </div>
      <div class="row">
        <div class="col-sm-8 col-lg-6 col-lg-offset-2">
          <% if content_for? :welcome %>
            <%= yield :welcome %>
          <% end %>

          <%= yield %>
        </div>
        <%= render partial: "layouts/sidebar" %>
      </div>
      <footer class="page-footer">
        <% if content_for? :footer %>
          <%= yield :footer %>
        <% else %>
          <%= render 'layouts/footer' %>
        <% end %>
      </footer>
    </div>
    <%= render 'layouts/javascripts' %>
    <%= render 'layouts/analytics' %>
    <!-- <script src="//js.pusher.com/2.2/pusher.min.js" type="text/javascript"></script> -->
  </body>
</html>
