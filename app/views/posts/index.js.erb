$("#posts-container .posts-scroll-placeholder").append('<%= escape_javascript(render partial: @posts) %>');
$('.pagination').html('<%= escape_javascript(link_to_next_page @posts, "Load More", class: "btn btn-primary", remote: true) %>');
